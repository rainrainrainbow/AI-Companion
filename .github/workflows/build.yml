name: Build AI Companion APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      build_type:
        description: 'Build type'
        required: true
        default: 'debug'
        type: choice
        options:
          - debug
          - release

env:
  BUILD_TYPE: ${{ inputs.build_type || 'debug' }}

jobs:
  build-apk:
    name: Build APK
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10'
          validate-wrappers: false

      - name: Generate Gradle Wrapper
        run: |
          gradle wrapper --gradle-version 8.10
          chmod +x gradlew

      - name: Setup Android SDK & NDK
        run: |
          echo "y" | ${ANDROID_SDK_ROOT}/cmdline-tools/latest/bin/sdkmanager --install "ndk;27.0.12077973" 2>/dev/null || true
          echo "NDK ready"

      - name: Clone llama.cpp
        run: |
          git clone --depth 1 --branch b4667 https://github.com/ggerganov/llama.cpp.git llama.cpp
          ls llama.cpp/CMakeLists.txt

      - name: Prepare native lib dirs
        run: |
          mkdir -p app/src/main/jniLibs/arm64-v8a
          mkdir -p app/src/main/jniLibs/armeabi-v7a

      - name: Build APK with NDK
        run: |
          if [ "$BUILD_TYPE" = "release" ]; then
            ./gradlew assembleRelease -x lint -x test --no-daemon
          else
            ./gradlew assembleDebug -x lint -x test --no-daemon
          fi

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: ai-companion-${{ env.BUILD_TYPE }}.apk
          path: app/build/outputs/apk/**/*.apk
          if-no-files-found: warn
